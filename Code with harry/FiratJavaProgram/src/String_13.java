public class String_13 {
    public static void main(String[] args) {
        int a = 23;
        float b = 3.4444f;
        System.out.printf("The value of a is %d and the value of b if %f", a,b);
        //if we are using %d or %f we should not use println statement.
        //instead we should use "printf" or "format"
        //%d = int
        //%f = float
        //%c = char
        //%s = string
    }
}
